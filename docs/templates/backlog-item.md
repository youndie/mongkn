---
id: B-NN
title: "Короткая формулировка задачи"
status: open          # open | wip | done | question | dropped
priority: P1          # P0 | P1 | P2 | P3 | infra
size: M               # XS | S | S/M | M | L | XL
stage: <slug этапа>   # поле, а не папка — см. STAGES в scripts/backlog_index.py
epic: feature-<name>  # необязательно: id фичи, к которой относится задача
blocked_by: [B-NN]    # необязательно: только то, что реально блокирует
---

# B-NN — Короткая формулировка задачи

Что не так сейчас и почему это стоит делать. Одним абзацем, с фактами из кода, а не общими
словами: какой файл, какое поведение, что видит пользователь.

- **Решение и его причина.** Не «сделать X», а «сделать X, потому что Y». Через полгода ценность
  задачи в том, почему выбрали именно так, — реализацию видно и в диффе.
- Альтернатива, которую отбросили, и чем она хуже.
- Что специально **не** делаем в этой задаче.

- AC: наблюдаемый результат. «Пользователь делает A → видит B», а не «реализовано A».
- Якоря: пути к файлам, которые придётся трогать (`repo/path/File.kt`).

<!--
Связи:
  · «связано» — обычной ссылкой в тексте: [B-12](B-12-slug.md)
  · «блокирует» — полем blocked_by выше (его читает индекс)
Ссылки на документы — с `../`: файл лежит на уровень ниже корня.
После правки: python3 scripts/backlog_index.py
-->
