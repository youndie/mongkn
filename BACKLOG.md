# BACKLOG

Порядок вех выведен из [ресёрча](docs/research/research-architecture.md), раздел 4: раньше всего
фиксируется то, от чего зависит форма всех остальных сигнатур.

Веха закрывается целиком и получает строчку итога: что вышло сверх плана и какая гипотеза
ресёрча подтвердилась или нет.

**Номер вехи — не приоритет, а порядок появления.** Ссылки на `M-NN` живут в коде и документах,
поэтому идентификаторы не переезжают.

Вехи M0–M8 закрыты: работает вертикаль — cinterop, память, потоки, BSON, маппинг классов,
публикация, CI. Дальше остаётся объём, и он разложен по стоимости:

| Веха | Про что | Стоимость |
|---|---|---|
| M9 | операции по образцу существующих | низкая, ~15 строк на операцию |
| M10 | опции операций и настройки коллекции | низкая, всё ключи в `opts` |
| M11 | база и клиент, прежде всего `runCommand` | низкая, но разблокирует M13 |
| M12 | агрегации | средняя, ложатся на механизм курсора |
| M13 | индексы | средняя, зависит от M-51 |
| M14 | сессии и транзакции | **высокая**, сталкивается с решением Р2 |
| M15 | change streams и bulkWrite | высокая, иной жизненный цикл курсора |
| M16 | ретраи, коды ошибок, логирование | средняя |
| M17 | **дыры в верификации**, а не в коде | разная; см. M-65 про аутентификацию |

Сколько реализовано сейчас — [docs/coverage.md](docs/coverage.md), там подсчёт по jar
официального драйвера.

---

## M0 — окружение и cinterop ✅

- [x] **M-01** Скелет проекта: Gradle 9.5.0 (потолок KGP 2.4.10), version catalog, `.gitignore`
- [x] **M-02** `cinterop` к mongo-c-driver с разрешением путей в Gradle вместо хардкода в `.def`
- [x] **M-03** `Mongkn.initialize/shutdown` под счётчиком ссылок + тест на версию рантайма

**Итог M0.** Сверх плана: пришлось целиком переписать способ линковки — драфтовые
`-lmongoc-1.0` и `-I/opt/homebrew/include` не работают с установленной 2.1.1, а pkg-config
в системе отсутствует. Подтвердилось прогоном: cinterop собирается, бинарник запускается,
полный путь `ping → insert → ошибка дубликата → курсор` против `mongo:8` проходит
(ресёрч §1.2, §1.3) — то есть архитектурных препятствий на уровне C нет.

## M1 — модель BSON ✅

- [x] **M-04** `BsonValue` sealed-иерархия + `Document`; тест round-trip `Document → bson_t → Document`
      с проверкой равенства, включая различие `BsonInt32` / `BsonInt64` и порядок ключей
      (сценарий «round-trip BSON без потери типов»)
- [x] **M-05** **Минимальный** билдер документов — прямая плата за отказ от `Map<String, Any>`
      (решение Р4): без него фильтр пишется как `Document("age" to BsonDocument("$gt" to BsonInt32(18)))`.
      Это **не** язык запросов: infix-операторы, property-references и прочий DSL — веха M7,
      после того как заработает `find` и станет видно, какие операторы реально нужны (Р7)
- [x] **M-06** Проверка отсутствия утечек: аллокатор libbson подменён на считающий (§1.14).
      Гипотеза про `staticCFunction` подтвердилась. Кодек не теряет ни блока — включая путь
      исключения посреди сборки. **Сетевые операции не покрыты:** фоновые потоки мониторинга
      аллоцируют через ту же libbson, точный баланс дал бы мигающий тест. Проверка на пустышку
      встроена в набор отдельным тестом.
      Пока освобождение курсора проверяется косвенно: после отменённого `find` клиент остаётся
      рабочим (`MongoIntegrationTest.cancelling the flow does not break the client`)

**Итог M1.** Round-trip прошёл на всех типах MVP, включая различие int32/int64 и порядок
ключей, — решение Р4 подтвердилось. Неожиданность: cinterop отрендерил `bson_type_t` не
Kotlin-энумом, а `typealias` на `UInt` (ресёрч §1.8).

## M2 — ресурсная модель ✅

- [x] **M-07** `MongoClient` поверх `mongoc_client_pool_t`, `MongoDatabase`, `MongoCollection`;
      pop/push в `finally`, операции на `Dispatchers.IO`. **`Dispatchers.Default` запрещён** —
      ресёрч §1.4
- [x] **M-08** Таймауты как верхняя граница неотменяемого вызова — риск 2. Задаются в строке
      подключения; интеграционный тест на недоступный сервер их и проверяет. Своих дефолтов
      библиотека не навязывает — открытый вопрос закрыт в пользу «решает вызывающий»

**Итог M2.** Сверх плана переписан жизненный цикл драйвера: `mongoc_init()` после
`mongoc_cleanup()` не восстанавливает libmongoc, и счётчик ссылок из M-03 ронял процесс
по assertion в `_mongoc_handshake_freeze`. Вторая находка — `Dispatchers.IO` на Kotlin/Native
объявлен `internal`, поэтому клиент завёл собственный пул потоков. Обе — ресёрч §1.8.

## M3 — операции ✅

- [x] **M-09** `suspend fun insertOne(document: Document): InsertOneResult` с разбором `insertedId`
      из `reply` и `bson_destroy` ответа при любом исходе
- [x] **M-10** `fun find(filter: Document): Flow<Document>` с `mongoc_cursor_destroy` в `finally`
      и конверсией документа **до** эмиссии
- [x] **M-11** Интеграционные тесты против локального mongod: все сценарии из
      [feature-crud-mvp](docs/features/feature-crud-mvp.md). `domain=15` для недоступного
      сервера подтвердился

**Итог M3.** 19 тестов зелёные, из них 10 интеграционных против `mongo:8`. Гипотеза ресёрча
«архитектурных препятствий на уровне C нет» подтвердилась полностью: и запись с разбором
`insertedId`, и курсор в `Flow` с освобождением при отмене работают. Из открытых вопросов
не закрыт ни один — они все про форму API, а не про механику.

## M4 — приведение API к официальному драйверу ✅

- [x] **M-12** Открытый вопрос 2 закрыт: `FindFlow<T>` **реализует** `Flow<T>`, поэтому это
      не развилка, а порядок — апгрейд до чейнинга будет source-совместимым (решение Р8)
- [x] **M-13** Поверхность сверена по jar 5.9.1 (`javap`), расхождения записаны
      в [api-collection](docs/api/api-collection.md), раздел 3

**Итог M4.** Сверка по артефакту, а не по документации, сразу закрыла открытый вопрос,
который планировалось решать «при первой реальной генерации»: одна строка
`FindFlow<T> implements Flow<T>` превратила выбор в очередность.

## M5 — генератор ✅

- [x] **M-14** KSP 2.3.11 с Kotlin 2.4.10 **совместим** — риск 1 снят, отступление на рефлексию
      не понадобилось
- [x] **M-15** JVM-модуль `mongkn-codegen`: официальный драйвер на classpath, KotlinPoet печатает
      исходники в каталог, `mongkn-core` подключает его как `srcDir` (решение Р5).
      **Не** KSP внутри нативного модуля — там JVM-jar на classpath не попадает (ресёрч §1.5).
      Требование к форме вывода: сгенерированный API расширяется снаружи без правок в нём самом —
      параметры-фильтры принимают `Document`, точки расширения не закрыты `internal`/`final` (Р7)
- [x] **M-16** Рукописный `MongoCollection` удалён, его печатает генератор; реализация уехала
      в `CollectionOps`. Публичные вызовы в тестах не менялись — но правка тестов всё же
      понадобилась, и не из-за генератора: они не чистили базу между прогонами

**Итог M5.** Схема из драфта (KSP внутри нативного модуля) действительно нерабочая, но
сломалась не там, где ожидалось: KSP прекрасно резолвит jar, а спотыкается JVM-модуль,
пытаясь **скомпилировать** сгенерированный нативный код. Лечится исключением каталога
из задачи компиляции. Генератор печатает только поверхность API — cinterop остался
рукописным, и это же сделало ненужным отдельный тест на генератор: если он напечатает
не то, `:mongkn-core` не соберётся.

## M5а — проверка премисы генератора ✅

- [x] **M-25** Расширить генератор до шести операций (`insertMany`, `updateOne`, `deleteOne`,
      `countDocuments`) и **измерить**, сколько кода реально экономится
- [x] **M-26** Интеграционные тесты новых операций (16 интеграционных тестов всего)

**Итог M5а. Премиса не подтвердилась.** Замер (ресёрч §1.10): одна сгенерированная строка
на шесть рукописных. Покрытие генератор не удешевляет — реализацию на libmongoc всё равно пишут
руками, потому что C-драйвер неоднороден. Что подтвердилось: генератор ловит неоднозначность,
которую человек проглядит (у `updateOne` две перегрузки одинаковой длины — документом и
конвейером), и не даёт молча разойтись с официальным API. Формулировка Р7 исправлена.

- [x] **M-27** Судьба генератора решена: **убираем** (решение Р9). Помимо замера сработал более
      сильный довод — генератор гарантирует *форму* API, а не *поведение*, то есть отвечает
      не на тот вопрос. Официальный JVM-драйвер остаётся в проекте, но как эталон для
      дифференциальных тестов, а не как источник сигнатур

## M6 — соответствие и проверки корректности

Самая большая веха по смыслу: сейчас почти все утверждения о корректности держатся на ожиданиях
автора, а не на эталоне. Порядок внутри вехи — по отношению «ценность / стоимость».

- [x] **M-28** Дифференциальные тесты против официального JVM-драйвера. Реализованы тремя
      фазами вокруг одного mongod (в одном процессе JVM-драйвер и Native не свести):
      `seedDiffReference` → нативный `MongoDifferentialTest` → `verifyDiffWritten`.
      Проверено намеренной поломкой: подмена int32 на int64 роняет и фазу B, и фазу C
      с точным указанием поля
- [x] **M-29** Конкурентный стресс-тест пула: 200 одновременных операций на одном `MongoClient`
      при 16 потоках — вставки, смешанная нагрузка, поток падающих операций, 32 одновременных
      курсора, одновременное создание клиентов. Риск 5 снят: ни потерянных записей, ни порчи
      памяти. **Побочная находка (§1.12):** сломанный пул не краснеет, а вешает прогон —
      `runTest(timeout)` не прерывает блокировку внутри `mongoc_client_pool_pop`
- [x] **M-33** Генератор убран: `:mongkn-codegen` и `:mongkn-api-spec` удалены, KSP и KotlinPoet
      вычищены из сборки, `MongoCollection` стал рукописным рядом с `CollectionOps`.
      Чистая сборка: 34 с / 24 задачи → 30 с / 18 задач — выгода скромная, работа KSP была
      копеечной на фоне Kotlin/Native. Главное не секунды, а что `:mongkn-core` снова
      собирается сам по себе
- [x] **M-30** Раннер spec-тестов MongoDB (unified test format) — частичный и честно об этом
      сообщающий: 5 сценариев выполнено, 17 пропущено с указанием причины, отчёт печатает
      операции **без** spec-покрытия. Файлы не вендорятся — CC BY-NC-SA (§1.16), качаются
      задачей `fetchSpecTests` в `build/`. Нашёл пробел: `insertMany` не возвращал `insertedIds`
      (§1.17), починено клиентской генерацией `_id`
- [x] **M-34** Spec-покрытие выросло с 5 до **14** сценариев; операций под покрытием пять
      из шести. Понадобилось: `FindFlow` с `limit`/`skip`/`sort`/`projection`/`batchSize`
      (заодно закрыта вторая половина решения Р8), `insertMany(ordered)`, `updateOne(upsert)`
      и вычисление `runOnRequirements` по версии сервера — без него `updateOne.json` целиком
      отсеивался требованием `minServerVersion: 2.6`
- [ ] **M-39** Оставшиеся 8 сценариев требуют `expectEvents`, то есть мониторинга команд
      (`mongoc_apm_callbacks_t`). Это отдельная подсистема ради одних тестов — браться, только
      если понадобится наблюдаемость и в проде
- [ ] **M-40** `countDocuments` без spec-покрытия: простого файла под него в наборе нет, только
      `countDocuments-comment` и `-rawdata` с неподдержанными аргументами
- [x] **M-35** Сопоставление в раннере усилено: лишние поля допускаются **только в корне**
      результата операции, во вложенных документах и в `outcome` сравнение строгое. Матчер
      вынесен в `SpecMatcher` и покрыт прямыми тестами — до этого строгость держалась на слове:
      все сценарии проходили и до ужесточения, и после, потому что ни один не содержит лишнего
      поля во вложенном документе

## M7 — эргономика ✅

Порядок внутри вехи жёсткий: property-reference нотация невозможна без типизированных
коллекций, а те — без маппинга data-классов.

- [x] **M-21** `MongoCollection<T>` поверх `kotlinx.serialization`: свой древесный формат
      с промежуточным `BsonValue` (решение Р10). Закрыт открытый вопрос 1. Полиморфизм
      и контекстная сериализация намеренно не поддержаны и падают с внятным сообщением
- [x] **M-22** Модуль `:mongkn-extensions`: infix-DSL фильтров (`eq`, `ne`, `gt`, `gte`, `lt`,
      `lte`, `within`, `exists`, `and`/`or`/`not`) и обновлений (`setTo`, `incBy`, `unset`,
      `combine`). Отдельный артефакт, а не часть ядра (Р7)
- [x] **M-36** Ссылки на свойства перенесены в `FilterScope` / `UpdateScope`, где известен
      дескриптор класса. Разрешить `@SerialName` по-прежнему нельзя (аннотация не доживает
      до дескриптора, рефлексии на Native нет), но расхождение теперь **обнаруживается**:
      вызов падает и перечисляет реальные имена полей. Обходной путь — строковые перегрузки,
      они остались (решение Р14)

## M8 — выпуск ✅

- [x] **M-17** CI-матрица macOS (mongo-c-driver 2.x) + Linux (libmongoc 1.x) и образ
      [ci/Dockerfile](ci/Dockerfile) для локальной проверки Linux. Ветка 1.x впервые исполнилась
      и оказалась сломанной в четырёх местах — резолвер библиотек (multiarch, суффикс `-1.0`,
      `libmongocrypt`), линковка против системной libbson и `insert_one` без `insertedId` (§1.18).
      Всё починено, 53 теста зелёные на обеих платформах.
      **Сам workflow не прогонялся** — GitHub Actions отсюда не запустить; проверено только то,
      что Linux-сборка проходит в контейнере
- [x] **M-18** Публикация в приватный Reposilite: `ru.workinprogress.mongkn`, модули
      `mongkn-core` и `mongkn-extensions` (плюс таргетные `*-macosarm64` / `*-linuxx64`).
      Креды — `REPOSILITE_USER` / `REPOSILITE_SECRET` из окружения или Gradle-свойств.
      `:mongkn-difftest` не публикуется — это тестовая оснастка.
      **Лицензия не выбрана** и секции `licenses` в POM намеренно нет: репозиторий приватный

---

## M9 — операции по образцу существующих ✅

- [x] **M-41** `deleteMany`, `updateMany`
- [x] **M-42** `replaceOne` — замена целиком, а не операторы обновления; `$`-ключи отвергает
      сервер, и его ошибка доходит до вызывающего как есть (проверено тестом)
- [x] **M-43** `findOneAndUpdate`, `findOneAndDelete`, `findOneAndReplace` — вместе с `sort`
      и `projection`. Сделаны **командой** `findAndModify`, а не через `mongoc_find_and_modify_opts_t`:
      структура опций потребовала бы набора сеттеров под каждый флаг, а команда — тот же документ
- [x] **M-44** `estimatedDocumentCount`
- [x] **M-45** `distinct` — отдельной функции у libmongoc нет, только команда. На этом же
      механизме (`read_command_with_opts`) потом встанет `runCommand` (M-51)
- [x] **M-46** `drop` и `renameCollection`

**Итог M9.** Операций стало 15 из 30 вместо 6 — половина. Официальное spec-покрытие выросло
**с 14 сценариев до 44**, и все оставшиеся пропуски теперь одного сорта: `expectEvents`, то есть
APM (M-39). Без spec-покрытия остались только `countDocuments` и `estimatedDocumentCount` —
простых файлов под них в наборе нет.

**Поправка к плану.** У M-44 было записано «разблокирует 3 официальных сценария» — это оказалось
неверно: все шесть сценариев `estimatedDocumentCount.json` требуют `expectEvents`, и реализация
операции их не разблокировала. Проверять надо было по содержимому файла, а не по его имени.

## M10 — опции операций

Закрыта, кроме M-50. Настройки коллекции вливаются в опции каждой операции, а параметр
`options` самой операции перекрывает их поключно.

Грабля, которую вскрыл этот заход: `insertOne` и всё семейство `findOneAnd*` передавали
в libmongoc `null` вместо документа опций, то есть **молча теряли** любую настройку. Отсюда
правило: тест на опцию обязан задавать значение, которое сервер **отвергает**. На принимаемом
значении такой тест зеленеет одинаково и когда опция доехала, и когда её выбросили по дороге.
Первая версия теста брала `writeConcern: {w: 50}` — standalone принимает его молча, и баг
остался бы незамеченным.

- [x] **M-47** Оставшиеся методы `FindFlow`: `hint`, `hintString`, `collation`, `comment`, `let`,
      `max`, `min`, `maxTime`, `maxAwaitTime`, `noCursorTimeout`, `partial`, `returnKey`,
      `showRecordId`, `allowDiskUse`, `cursorType` — всё это ключи в том же документе `opts`.
      Не сделан `timeoutMode`: он часть CSOT, а не отдельный ключ
- [x] **M-48** Опции операций записи — параметром `options: Document` у каждой операции записи.
      Раньше в `opts` уезжали только `ordered` и `upsert`
- [x] **M-49** Настройки коллекции: `withWriteConcern`, `withReadConcern`, `withTimeout`,
      каждая возвращает копию. `withReadPreference` отложен вместе с M-50
- [ ] **M-50** `readPreference` при чтении — `mongoc_collection_find_with_opts` принимает его
      отдельным параметром, куда мы сейчас передаём `null`. На standalone непроверяем:
      нужен replica set (см. M17)

## M11 — база и клиент

Закрыта. Осталось то, что делается через `runCommand` и потому не блокирует.

- [x] **M-51** `MongoDatabase.runCommand` — на `mongoc_database_command_with_opts`. Через неё
      делается всё, чего нет в API; тестам больше не нужно лезть в cinterop напрямую
- [x] **M-52** `MongoDatabase`: `createCollection`, `drop`, `listCollectionNames`. `createView`
      и `listCollections` с полными документами не делались: обе доступны через `runCommand`
- [x] **M-53** `MongoClient`: `listDatabaseNames`. `listDatabases` с полными документами — там же,
      где `listCollections`

## M12 — агрегации

- [ ] **M-54** `MongoCollection.aggregate` и `MongoDatabase.aggregate` — `mongoc_collection_aggregate`.
      Возвращают курсор, то есть ложатся на существующий механизм `find`; новое здесь — конвейер
      как `List<Document>`
- [ ] **M-55** Разобраться с `AggregateFlow`: у официального драйвера это отдельный тип
      с чейнингом, как `FindFlow`. Решить, повторять ли форму (см. решение Р8)

## M13 — индексы

- [ ] **M-56** `createIndex`, `createIndexes`, `dropIndex`, `dropIndexes`, `listIndexes`.
      В libmongoc это команды, а не отдельные функции, — значит зависит от M-51
- [ ] **M-57** Поисковые индексы Atlas (`createSearchIndex` и соседи) — решить, нужны ли вообще:
      работают только в Atlas, локальным mongod не проверяются

## M14 — сессии и транзакции

Первая по-настоящему дорогая веха: у официального драйвера `ClientSession` — перегрузка
**каждой** операции.

- [ ] **M-58** `ClientSession` поверх `mongoc_client_session_t`: время жизни, привязка к клиенту
      из пула. Осторожно: сессия принадлежит клиенту, а клиент у нас живёт только на время
      операции — это прямое столкновение с решением Р2, и его надо решить до реализации
- [ ] **M-59** Транзакции: `startTransaction`, `commitTransaction`, `abortTransaction`,
      `withTransaction`. Требуют реплика-сета — локальный standalone mongod не подойдёт,
      нужен отдельный контур в CI

## M15 — потоки изменений и bulk

- [ ] **M-60** `watch` (change streams) — курсор с иным жизненным циклом: он не заканчивается,
      а ждёт. Проверить, как это ложится на удержание клиента из пула (см. решение Р2)
- [ ] **M-61** `bulkWrite` — `mongoc_bulk_operation_t`; отдельная модель операций и результата

## M16 — наблюдаемость и надёжность

- [ ] **M-62** Ретраи: спецификация MongoDB описывает retryable reads и writes. Сегодня их нет,
      и это молчаливое отличие от официального драйвера — при обрыве соединения он повторит,
      а мы отдадим ошибку
- [ ] **M-63** Покрытие кодов ошибок: `mongoc_error_domain_t` содержит 22 домена, а мы видели
      два (12 и 15). Остальные не проверялись ни разу
- [ ] **M-64** Структурированное логирование libmongoc (`mongoc_structured_log_opts_t`) —
      если понадобится диагностика в проде

## M17 — непроверенные области

Не фичи, а дыры в **верификации**: код может работать, а может и нет — мы не знаем.

- [ ] **M-65** Аутентификация не проверялась **ни разу**: весь прогон идёт против mongod без
      учётных данных. SCRAM, x509, TLS — всё это в строке подключения и полностью непокрыто
- [ ] **M-66** Только standalone-сервер. Реплика-сет и шардированный кластер не проверялись,
      а `runOnRequirements` в spec-тестах различает топологии — часть сценариев мы пропускаем
      именно поэтому
- [ ] **M-67** Производительность не измерялась вовсе. Модель `BsonValue` аллоцирует объект
      на каждое значение (осознанная цена решения Р4); прежде чем оптимизировать — замерить.
      Настоящий рычаг, если понадобится, — читать из `bson_iter_t` сразу в пользовательский тип,
      минуя дерево, а не менять модификаторы у классов
- [ ] **M-68** Отмена **выполняющейся** операции не проверялась: тесты отменяют сбор потока,
      но не вызов в середине. Отменить блокировку внутри C нечем (§1.12), и стоит убедиться,
      что это выглядит как задержка до таймаута, а не как порча состояния
- [ ] **M-69** Поведение при падении сервера посреди курсора — что увидит потребитель `Flow`

## Вне вех


- [x] **M-70** ktlint в гейте: плагин `org.jlleitschuh.gradle.ktlint` 14.2.0 с явно заданной
      версией CLI `1.8.0`, стиль `ktlint_official` в `.editorconfig`. `ktlintCheck` цепляется
      к `check`, то есть попадает в `./gradlew build` без правок CI. Приведено 455 нарушений;
      по дороге ktlint нашёл **два дублирующихся блока KDoc**, оставшихся от правок M-34, —
      компилятор их не замечал
- [x] **M-37** ~~Публикация покрывает только тот таргет, на котором собрана.~~ **Снято
      требованием:** целевая платформа одна — `linuxX64`, macOS-таргет не публикуется.
      Публикует всегда Linux, конфликта метаданных не возникает. Вернуть в работу, если
      появятся потребители на другой платформе

- [x] **M-23** Перед пулом поставлен `Semaphore` с числом разрешений ровно в
      `mongoc_client_pool_max_size` (§1.13). Ожидание клиента стало приостановкой: отменяемо,
      `withTimeout` работает, поток свободен. **Само удержание клиента курсором осталось** —
      `mongoc_cursor_t` принадлежит клиенту, и это ограничение libmongoc снять нельзя; изменился
      режим отказа: было зависание намертво, стал backpressure. Появился риск 6: инвариант
      держится на одном `finally`
- [x] **M-24** Покрытие типов BSON доведено до полного, кроме `dbpointer` и `code with scope`
      (оба устаревшие). Добавлены `BsonBinary` с подтипом, `BsonDecimal128`, `BsonTimestamp`,
      `BsonRegex`, `BsonCode`, `BsonSymbol`, `BsonUndefined`, `BsonMinKey`, `BsonMaxKey`.
      Все сверены с официальным драйвером — дифференциальный эталон вырос до 25 полей
- [ ] **M-38** Мост `BsonDecimal128` ↔ BigDecimal, если понадобится арифметика. **Только
      в `:mongkn-extensions`**, не в ядре: это `api`-зависимость для всех потребителей,
      а преобразование не взаимно однозначно — `NaN`, `±Infinity`, отрицательный ноль
      и значащие хвостовые нули (решение Р13)

- [x] **M-20** ~~Сделать разрешённые пути к заголовкам явным входом задачи `cinterop`.~~
      **Не требуется:** KGP уже ведёт свою проверку — карту «путь → хеш содержимого» в
      `cinterop-headers-hash.json`. `brew upgrade` меняет и путь, и хеш, задача пересобирается
      сама. Риск 4 снят как несуществующий

## Вне скоупа осознанно

Записано, чтобы не возвращаться к вопросу «а почему этого нет»:

* **GridFS** — отдельная подсистема поверх драйвера, к ядру отношения не имеет.
* **Client-side field level encryption** — требует libmongocrypt и отдельного контура ключей.
* **`mapReduce`** — устарел, MongoDB рекомендует агрегации.
* **Публикация под macOS** — целевая платформа одна, `linuxX64` (M-37).
